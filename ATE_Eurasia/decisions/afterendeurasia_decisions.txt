decisions = {
	hunt_for_whale = {
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 3
		potential = {
			is_playable = yes
			is_adult = yes
			prisoner = no
			religion = pelagic
			NOT = {	has_character_flag = on_whale_hunt }
			NOT = {	has_character_modifier = recent_whaling }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			war = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			month = 6
			NOT = {
				month = 8
			}
			wealth = 50
			prisoner = no
			NOT = { is_inaccessible_trigger = yes }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			piety = 20
			set_character_flag = on_whale_hunt
			set_character_flag = do_not_disturb
			hidden_tooltip = { character_event = { id = pelagic.10 days = 150 } } # Safety catch flag clearing
			custom_tooltip = {
				text = whale_hunt
				hidden_tooltip = {
					character_event = { id = pelagic.1 } 
				}
			}
			if = {
				limit = {
					has_dlc = "Reapers"
				}
				custom_tooltip = {
					text = capital_prospers_custom_tooltip
					hidden_tooltip = {
						capital_scope = {
							change_variable = { which = prosperity_value value = 8 }
						}
					}
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0
				NOT = {
					wealth = 100
				}
			}
			modifier = {
				factor = 0
				trait = craven
			}	
			modifier = {
				factor = 0
				has_character_flag = recent_minor_decision
				NOT = {
					had_character_flag = {
						flag = recent_minor_decision
						days = 730
					}
				}
			}
		}
	} 

	celebrate_st_patricks_day = {
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 3
		potential = {
			is_playable = yes
			is_adult = yes
			prisoner = no
			OR = {
				religion = insular
				religion = apparitionist
				culture = irish
				culture = pavee
			}
			NOT = { has_character_modifier = st_patricks_timer }
			NOT = {	has_character_flag = st_patricks_day }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		
		allow = {
			war = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			month = 2
			NOT = { month = 3 }
			wealth = 50
			prisoner = no
			NOT = { is_inaccessible_trigger = yes }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		
		effect = {
			wealth = -50
			set_character_flag = st_patricks_day
			set_character_flag = recent_minor_decision
			set_character_flag = do_not_disturb
			hidden_tooltip = { character_event = { id = canada.39 days = 30 } } # Safety catch flag clearing
			custom_tooltip = {
				text = TXT_st_patricks_day_decision
				hidden_tooltip = { 
					character_event = { id = canada.30 } 
				}
			}
			if = {
				limit = {
					has_dlc = "Reapers"
				}
				custom_tooltip = {
					text = capital_prospers_custom_tooltip
					hidden_tooltip = {
						capital_scope = {
							change_variable = { which = prosperity_value value = 4 }
						}
					}
				}
			}
		}
			
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1	
			modifier = {
				factor = 0
				NOT = {
					wealth = 200
				}
			}
			modifier = {
				factor = 0
				trait = cynical
			}	
			modifier = {
				factor = 0
				has_character_flag = recent_minor_decision
				NOT = {
					had_character_flag = {
						flag = recent_minor_decision
						days = 730
					}
				}
			}
		}
	}
	
	change_jacobin_branch = {
		ai = no
		
		potential = {
			religion = jacobin
			is_playable = yes
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			NOT = { has_character_flag = changedjacobinbranch }
		}
		
		allow = {
			religion = jacobin
			is_playable = yes
			NOT = { has_character_flag = changedjacobinbranch }
		}
		
		effect = {
			hidden_tooltip = {
				character_event = { id = jacobin.1 }		
			}
			custom_tooltip = { text = TXT_change_jacobin_branch }
			set_character_flag = changedjacobinbranch
		}
		
		revoke_allowed = {
			always = no
		}
	}
	
	celebrate_yule = {
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 3
		
		potential = {
			is_playable = yes
			is_adult = yes
			prisoner = no
			OR = {
				culture_group = finnic
				culture_group = sami_group
				culture = vestlending
				culture = ostlending
				culture = jamta
				culture = tronder
				culture = swedish
			}
			NOT = { has_character_flag = celebrating_yule }
		}
		allow = {
			war = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			month = 11
			NOT = { month = 12 }
			wealth = 50
			prisoner = no
			NOT = { is_inaccessible_trigger = yes }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			wealth = -50
			set_character_flag = recent_minor_decision
			set_character_flag = do_not_disturb
			hidden_tooltip = { character_event = { id = yule.1 days = 1 } }
			if = {
				limit = {
					has_dlc = "Reapers"
				}
				custom_tooltip = {
					text = capital_prospers_custom_tooltip
					hidden_tooltip = {
						capital_scope = {
							change_variable = { which = prosperity_value value = 4 }
						}
					}
				}
			}
		}
		ai_will_do = {
			modifier = {
				factor = 0
				NOT = {wealth = 200}
			}
			modifier = {
				factor = 0
				trait = cynical
			}	
			modifier = {
				factor = 0
				has_character_flag = recent_minor_decision
				NOT = {
					had_character_flag = {
						flag = recent_minor_decision
						days = 730
					}
				}
			}
		}
	}
	become_solarque = {
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 12
		
		potential = {
			religion = sun_king_cultist
			d_sun_king_cultist = { has_holder = no }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			primary_title = { higher_tier_than = DUKE }
		}
		
		allow = {
			religion = sun_king_cultist
			has_landed_title = k_france
			num_of_holy_sites = 3
			piety = 150
			wealth = 100
		}
		
		effect = {
			wealth = -100
			piety = -150
			set_global_flag = solarque_reborn
			activate_title = { title = d_sun_king_cultist status = yes }
			d_sun_king_cultist = { grant_title = ROOT }
			narrative_event = { id = skc.1 }	
		}
		
		ai_will_do = {
			factor = 1
		}
	}
	select_jedinstvo_deity = {
		is_high_prio = yes
		
		potential = {
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			NOT = {
				has_character_flag = selecting_jedinstvo_deity
			}
			religion = jedinstvo
			is_adult = yes
			has_jedinstvo_deity_trigger = no
		}
		allow = {
			block_general_event_trigger = no
			piety = 100
		}
		effect = {
			character_event = { id = jedinstvo.27 }
			set_character_flag = selecting_jedinstvo_deity
			piety = -100
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0.3 # Slow it down a little
			}
		}
	}
	jedinstvo_pilgrimage = {
		is_high_prio = yes
		only_playable = yes
		ai_check_interval = 120
		
		potential = {
			religion = jedinstvo
			age = 16
			prisoner = no
			in_seclusion = no
			NOT = { is_incapable = yes }
			NOT = { trait = pilgrim }
			NOT = { trait = on_pilgrimage }
			NOT = { trait = legendary }
			NOT = { #immersion-breaking
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			war = no
			piety = 100
			wealth = 50
		}
		effect = {
			add_trait = on_pilgrimage
			wealth = -50
			hidden_tooltip = {
				character_event = {
					id = jedinstvo.39
				}
			}
			if = {
				limit = {
					has_dlc = "Reapers"
				}
				custom_tooltip = {
					text = capital_prospers_custom_tooltip
					hidden_tooltip = {
						capital_scope = {
							change_variable = { which = prosperity_value value = 8 }
						}
					}
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			modifier = { factor = 0.001 }
			modifier = { factor = 10 num_of_count_titles = 1 }
			modifier = { factor = 2	trait = zealous }
			modifier = { factor = 2	trait = brave }
			modifier = { factor = 2	trait = ambitious }
			modifier = { factor = 0 trait = cynical }
			modifier = { factor = 0 trait = craven }
			modifier = { factor = 0 trait = content }
			modifier = { factor = 0 NOT = { wealth = 250 } }
		}
	}
	aid_harvest = {
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 6

		potential = {
			is_adult = yes
			prisoner = no
			religion = navayana
			NOT = { has_character_modifier = aided_harvest }
		}
		allow = {
			is_adult = yes
			prisoner = no
			religion = navayana
			custom_tooltip = {
				text = TXT_not_sick_or_wounded_or_lazy
				hidden_tooltip = {
					NOR = {
						trait = wounded
						trait = maimed
						trait = slothful
						trait = mangled
						trait = one_legged
						trait = severely_injured
						trait = has_aztec_disease
						trait = has_typhus
						trait = cancer
						trait = has_tuberculosis
						trait = dancing_plague
						trait = dysentery
						trait = flu
						trait = food_poisoning
						trait = gout
						trait = syphilitic
						trait = has_typhoid_fever
						trait = ill
						trait = leper
						trait = has_measles
						trait = rabies
						trait = scurvy
						trait = has_small_pox
						trait = has_bubonic_plague
					}
				}
			}
			month = 6
			NOT = { month = 5 }
		}
		effect = {
			character_event = { id = budevents.8 }
		}
		ai_will_do = {
			factor = 1

			modifier = {
				factor = 0
				NOT = {
					trait = cynical
				}
			}
		}
	}
	join_khalsa = {
		is_high_prio = yes
		ai_check_interval = 6

		potential = {
			age = 15
			religion = sikh
			NOT = { trait = amritdhari }
			NOT = { has_character_flag = initiated_into_khalsa }
		}
		allow = {
			age = 15
			religion = sikh
			NOT = { trait = amritdhari }
		}
		effect = {
			character_event = { id = sikh.1 days = 1 }
		}
	}
	pilgrimage_to_the_takhts = { #and other Takhts
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 60
		potential = {
			is_playable = yes
			religion = sikh
			NOT = { trait = travelling }
			NOT = {	has_character_flag = visiting_golden_temple }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			age = 16
			prisoner = no
			scaled_wealth = 0.2
			war = no
			in_command = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			
			NOT = { is_inaccessible_trigger = yes }
			custom_tooltip = {
				text = health_travel_condition
				hidden_tooltip = {
					NOT = {	is_incapable = yes }
					NOT = {	trait = blinded }
				}
			}
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			scaled_wealth = -0.2
			hidden_tooltip = {
				narrative_event = { id = sikh.2 days = 1 }
				add_trait = travelling
			}
			set_character_flag = visiting_golden_temple
			set_character_flag = recent_minor_decision
			set_character_flag = do_not_disturb
			
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0
				trait = cynical
			}
			modifier = {
				factor = 0
				NOT = { wealth = 300 }
			}
			modifier = { # Monthly randomizer for when AI takes decision (1% chance)
				factor = 0
				random = 1
			}
			modifier = {
				factor = 0
				NOT = { age = 30 }
			}
		}
	}
	go_on_yatra = { #Modification of the Vanilla Event
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 60
		potential = {
			is_playable = yes
			is_hinduism = yes
			has_dlc = "Mystics"
			OR = {
				NOT = { has_dlc = "Way of Life" }
				has_focus = focus_theology
			}
			NOT = { trait = yatri }
			NOT = { trait = on_yatra }
			NOT = {	has_character_flag = preparing_yatra }
			NOT = {	has_character_flag = activated_yatra }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			age = 16
			prisoner = no
			scaled_wealth = 0.2
			war = no
			in_command = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			
			NOT = { is_inaccessible_trigger = yes }
			custom_tooltip = {
				text = health_travel_condition
				hidden_tooltip = {
					NOT = {	is_incapable = yes }
					NOT = {	trait = blinded }
				}
			}
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			scaled_wealth = -0.2
			hidden_tooltip = {
				narrative_event = { id = hindu.30 }
			}
			set_character_flag = activated_yatra
			set_character_flag = recent_minor_decision
			set_character_flag = do_not_disturb
			
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0
				trait = cynical
			}
			modifier = {
				factor = 0
				NOT = { wealth = 300 }
			}
			modifier = { # Monthly randomizer for when AI takes decision (1% chance)
				factor = 0
				random = 1
			}
			modifier = {
				factor = 0
				NOT = { age = 30 }
			}
		}
	}
	go_on_yatra_jain = { #Modification of the Vanilla Event
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 60
		potential = {
			is_playable = yes
			religion = jain
			has_dlc = "Mystics"
			OR = {
				NOT = { has_dlc = "Way of Life" }
				has_focus = focus_theology
			}
			NOT = { trait = yatri }
			NOT = { trait = on_yatra }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			age = 16
			prisoner = no
			scaled_wealth = 0.2
			war = no
			in_command = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			
			NOT = { is_inaccessible_trigger = yes }
			custom_tooltip = {
				text = health_travel_condition
				hidden_tooltip = {
					NOT = {	is_incapable = yes }
					NOT = {	trait = blinded }
				}
			}
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			scaled_wealth = -0.2
			hidden_tooltip = {
				character_event = { id = jain.8002 }
			}
			set_character_flag = recent_minor_decision
			set_character_flag = do_not_disturb
			
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0
				trait = cynical
			}
			modifier = {
				factor = 0
				NOT = { wealth = 300 }
			}
			modifier = { # Monthly randomizer for when AI takes decision (1% chance)
				factor = 0
				random = 1
			}
			modifier = {
				factor = 0
				NOT = { age = 30 }
			}
		}
	}
}
targeted_decisions = {						
	navjote = {
		only_playable = yes
		is_high_prio = yes
		
		filter = home_court
		ai_target_filter = home_court
		ai_check_interval = 12
	
		from_potential = {
			is_playable = yes
			religion = zoroastrian
			prisoner = no
			NOR = { 
				trait = incapable
				is_inaccessible_trigger = yes
			}
			war = no
		}
		
		potential = {
			is_alive = yes
			religion = zoroastrian
			NOT = {
				has_character_flag = navjoted
			}
			is_child_of = FROM
		}
		allow = {
			age = 15
			NOR = {
				is_landed = yes
				prisoner = yes
				is_adult = yes
				custom_tooltip = {
					text = bastard_eunuch_celibate_or_incapable_tt
					OR = {
						trait = bastard
						trait = eunuch
						trait = celibate
						trait = incapable
					}
				}
				is_ascetic_trigger = yes
			}
			is_inaccessible_trigger = no
			host = { character = FROM }
		}
		effect = {
			custom_tooltip = {
				text = navjote_custom_tooltip
				hidden_tooltip = {
					set_character_flag = navjoted
					save_event_target_as = navjote_target
					FROM = { character_event = { id = zoroastrian.1 } }
				}
			}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0.1
				always = yes
			}
		}
	}
}