
decisions={

    become_allfader = {
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 12
		
		potential = {
			religion = nysed
			d_nysed = { has_holder = no }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			primary_title = { higher_tier_than = COUNT }
		}
		
		allow = {
			primary_title = { higher_tier_than = DUKE }
			num_of_holy_sites = 3
			prestige = 500
		}
		
		effect = {
			wealth = -250
			piety = -200
			set_global_flag = nysed_reborn
			activate_title = { title = d_nysed status = yes }
			d_nysed = { grant_title = ROOT }
			narrative_event = { id = troll.4 }
			
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	choose_fornsed_branch = {
		is_high_prio = yes
		ai = no
		
		potential = {
			religion = fornsed
			is_playable = yes
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		
		allow = {
			religion = fornsed
			is_playable = yes
			piety = 50
		}
		
		effect = {
		    piety = -50
			hidden_tooltip = {
				character_event = { id = troll.16 }		
			}
		}
		
		revoke_allowed = {
			always = no
		}
	}
	
	choose_nysed_branch = {
		is_high_prio = yes
		
		potential = {
			religion = nysed
			is_playable = yes
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		
		allow = {
			religion = nysed
			is_playable = yes
			OR = {
			   piety = 50
			   AND = {
				    NOT = { trait="nysed_thor" }
					NOT = { trait="nysed_elves" }
					NOT = { trait="nysed_fiddler" }
					NOT = { trait="nysed_wild_hunt" }
				}
			}
		}
		
		effect = {
		    if = {
			   limit = { OR = { trait="nysed_thor" trait="nysed_elves" trait="nysed_fiddler" trait="nysed_wild_hunt" } }
		        piety = -50
			}
			hidden_tooltip = {
				character_event = { id = troll.29 }		
			}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 0

			modifier = {
				factor = 3
				AND = {
				    NOT = { trait="nysed_thor" }
					NOT = { trait="nysed_elves" }
					NOT = { trait="nysed_fiddler" }
					NOT = { trait="nysed_wild_hunt" }
				}
			}
		}
	}
	
	go_to_folketro_pilgrimage = {
		only_playable = yes
		ai_check_interval = 20
		potential = {
			has_dlc = "Sons of Abraham"
			OR = {
				NOT = { has_dlc = "Way of Life" }
				has_focus = focus_theology
				trait = zealous
				society_member_of = warrior_lodge_folketro
			}
			is_playable = yes
		    OR = {
			   religion = folketro
			   religion = fornsed
			}
			NOT = { trait = pilgrim }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			war = no
			in_command = no
			conditional_tooltip = {
				trigger = { has_character_modifier = coronation_request_cooldown }
				custom_tooltip = {
					text = TT_NOT_RECENTLY_ASKED_CORONATION
					hidden_tooltip = { 
						NOT = { has_character_modifier = coronation_request_cooldown } 
					} 
				} 
			} 
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			prisoner = no
			NOT = { is_inaccessible_trigger = yes }
			scaled_wealth = 0.2
			age = 16
			custom_tooltip = {
				text = health_travel_condition
				hidden_tooltip = {
					NOT = {	trait = pregnant }
					NOT = {	is_incapable = yes }
					NOT = {	trait = blinded }
					NOT = {	trait = consumption }
					NOT = {	trait = has_typhoid_fever }
					NOT = {	trait = has_typhus }
					NOT = {	trait = has_bubonic_plague }
					NOT = {	trait = has_measles }
					NOT = {	trait = has_small_pox }
					NOT = {	trait = has_aztec_disease }
				}
			}
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			scaled_wealth = -0.2
			set_character_flag = activated_pilgrimage
			set_character_flag = do_not_disturb
			hidden_tooltip = {
				character_event = { 
					id = troll.17
				}
			}
		}
		ai_will_do = {
			factor = 1

			modifier = {
				factor = 0.1 
			}
			modifier = {
				factor = 3
				trait = zealous
			}
			modifier = {
				factor = 0
				NOT = { wealth = 200 }
			}
			modifier = {
				factor = 0
				NOT = { age = 30 }
			}
		}
	}
	
	celebrate_st_lucy_day = {
		is_high_prio = yes
		ai = no
		
		potential = {
		    OR = {
			    religion = fornsed
				religion = folketro
				AND = {
				    OR = {
				       religion = lutheran
					   religion = seperatist
					   religion = penitant
					   religion = memento_mori
					   religion = catholic
					   religion = marcelianist
					   religion = constitutionalist
					   religion = apparitionist
					   religion = orthodox
					   religion = oldcalendarist
					   religion = antipalamite
					   religion = pravoslavie
					   religion = dvoeverie
					   religion = mariavite
					   religion = coronite
					   religion = oasite
					   religion = high_church
					   religion = apostolic
					}
					OR = {
					   culture = danish
					   culture = scanian
					   culture = gotalander
					   culture = svealander
					   culture = jamta
					   culture = sami
					   culture = tronder
					   culture = vestlending
					   culture = ostlending
					}
				}
			}
			month = 11
			is_playable = yes
			is_adult = yes
			prisoner = no
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			NOT = { has_character_modifier = st_lucys_timer }
			NOT = {	has_character_flag = st_lucys_day }
		}
		
		allow = {
			war = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			month = 11
			wealth = 25
			prisoner = no
			NOT = { is_inaccessible_trigger = yes }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		
		effect = {
			wealth = -50
			set_character_flag = st_lucys_day
			set_character_flag = recent_minor_decision
			set_character_flag = do_not_disturb
			hidden_tooltip = { character_event = { id = troll.50 } }
			if = {
				limit = {
					has_dlc = "Reapers"
				}
				custom_tooltip = {
					text = capital_prospers_custom_tooltip
					hidden_tooltip = {
						capital_scope = {
							change_variable = { which = prosperity_value value = 4 }
						}
					}
				}
			}
		}
		
		revoke_allowed = {
			always = no
		}
	}
}

targeted_decisions = {		
	folketro_confirmation = {
		only_playable = yes
		is_high_prio = yes
		
		filter = home_court
		ai_target_filter = home_court
		ai_check_interval = 12
	
		from_potential = {
			is_playable = yes
			OR = {
				religion = folketro
				religion = fornsed
			}
			prisoner = no
			NOR = { 
				trait = incapable
				is_inaccessible_trigger = yes
			}
			war = no
		}
		
		potential = {
			is_alive = yes
			OR = {
				religion = folketro
				religion = fornsed
			}
			NOT = {
				has_character_flag = confirmed				
			}
			is_child_of = FROM
			NOT = { has_character_flag = holding_confirmation }
		}
		allow = {
			age = 12
			NOR = {
				is_landed = yes
				prisoner = yes
				is_adult = yes
				custom_tooltip = {
					text = bastard_eunuch_celibate_or_incapable_tt
					OR = {
						trait = bastard
						trait = eunuch
						trait = celibate
						trait = incapable
					}
				}
				is_ascetic_trigger = yes
			}
			is_inaccessible_trigger = no
			host = { character = FROM }
		}
		effect = {
			custom_tooltip = {
				text = folketro_confirmation_custom_tooltip
				hidden_tooltip = {
					set_character_flag = confirmed
					set_character_flag = holding_confirmation
					save_event_target_as = confirmation_target
					FROM = { character_event = { id = troll.6 } }
					FROM = { character_event = { id = troll.7 days = 1 } }
				}
			}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}